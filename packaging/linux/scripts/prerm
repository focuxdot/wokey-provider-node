#!/bin/sh
set -eu

cat <<'EOF'
Removing Wokey Provider Node package.

When the package is removed through `wokey-node uninstall`, the per-user
service is stopped first. Direct package-manager removal cannot discover every
user service. User data is never removed by the package manager.
EOF

exit 0
